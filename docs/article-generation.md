# ブログ記事生成ガイド — Claude Code を使った下書き作成

> 対象：漢那さん  
> 更新日：2026-04-10

---

## 1. 概要

このドキュメントは、Claude Code を使ってビズシスのブログ記事の下書きを作成するための手順書です。

プロンプトテンプレートをコピーして Claude Code のチャットに貼り付けるだけで、`site/src/content/blog/` にそのまま置ける形式の記事下書きが生成されます。

---

## 2. ビズシスブログの記事要件

### ターゲット読者

- 中小企業の経営者・管理職
- 非エンジニア。IT リテラシーは低め
- 「Excel での管理に限界を感じているが、何から始めれば良いかわからない」層

### トーン

- 親しみやすく、専門的すぎない
- 「難しそう」という印象を取り除く
- 結論→理由→具体例の順で書く（忙しい経営者が流し読みできる）

### 記事構成（2026-06-19更新）

```
## はじめに
（読者の課題を提示 + 読むメリット箇条書き）

## 課題の具体例
（「あるある」な状況の描写）

## 解決策の提示
（ビズシスのアプローチ・具体的なステップ）

## システム化でできること（主な機能）
（具体的な機能名を箇条書き。機能名がイメージできる粒度で）
※「変更履歴」「更新履歴」など"いつ・誰が・どう変更したかを記録する監査ログ的機能"は実装コストが高いため、機能一覧に書かない（後述の注記参照）

## 小さく始めるなら、どの機能から
（一番手間がかかる業務ひとつを名指し。FAQと重複させない）

## 相談前に準備しておくとスムーズなもの
（Excel/帳票サンプル・困っている業務・規模感・予算希望。ハードルを上げない）

## よくある質問
（FAQ）

## まとめ・次のアクション
（要約 + 問い合わせへの誘導 + 関連ページ導線）
```

- **本文 3,500文字程度**（2026-06-19に3,000→3,500へ引き上げ）
- **まとめ末尾の関連ページ導線（サービス/事例/デモ/料金）はHTMLのカード形式で書く**（相対パス。事例リンクは記事テーマに近い事例がある時だけ）。リード文＋ `.page-links` カードで出力し、別タブで開く：
  ```html
  <p class="page-links-lead">あわせて、こちらもご覧ください。</p>

  <div class="page-links">
  <a target="_blank" rel="noopener" href="/services"><b>サービス内容</b>システム開発の概要・流れ</a>
  <a target="_blank" rel="noopener" href="/cases"><b>導入事例</b>他社の改善イメージ</a>
  <a target="_blank" rel="noopener" href="/demo"><b>操作デモ</b>実際の画面の動き</a>
  <a target="_blank" rel="noopener" href="/pricing"><b>料金</b>費用の目安</a>
  </div>
  ```
- 詳細は `docs/templates/blog_template.md` と memory `project-blog-spec` を正とする。

#### 集客記事として強くする方針（2026-06-30追加）

ChatGPTレビューを受けて合意した、SEO記事を「商談につながる記事」に近づける方針。汎用的に効くものだけルール化。

- **一般論を減らし、具体例・失敗例・改善策で書く（2026-06-30追加・この方針の土台）**
  - 「〜が大切です」「〜という課題があります」で終わらせない。**具体的な業務シーン → あるある失敗例 → それをどう改善するか**をセットで示す。読者が自分ごと化でき、商談につながりやすい（ChatGPT指摘「抽象論が多く現場のリアルな痛みが薄い」への対応）。各記事の「### 具体例：〜のイメージ」もこの一環。
  - **既存ルールと両立させる（ここがズレやすいので注意）**：
    - ①トーンは崩さない＝「親しみやすく・専門的すぎない・非エンジニア向け」のまま。**具体≠専門用語**（DB設計やコード詳細に踏み込まない。あくまで業務の具体）。
    - ②改善策は**「何が変わるか」の粒度**に留める。具体的にしようとして重い機能（変更履歴＝監査ログ等）を約束しない（後述の監査ログ禁止ルール参照）。
    - ③本文3,500字を膨らませない＝一般論を**"足す"のではなく具体例に"置き換える"**。

- **「○○しやすい業務例」セクションを本文中盤に入れ、既存記事へ内部リンクする（最重要）**
  - そのテーマで属人化・非効率になりやすい**具体業務（受注管理・在庫管理・案件管理・配車・生産管理・見積・営業日報・顧客管理 等）**を列挙し、各業務を対応する既存ブログ記事へ相対リンクする。
  - **出力は次のHTMLで書く**（カード表示はCSSの `.biz-links` が担当。markdownの箇条書きではなくこの形にする）：
    ```html
    <div class="biz-links">
    <a target="_blank" rel="noopener" href="/blog/対象スラッグ"><b>業務名</b>対象記事のタイトル</a>
    <a target="_blank" rel="noopener" href="/blog/対象スラッグ2"><b>業務名2</b>対象記事のタイトル2</a>
    </div>
    ```
    `<b>` が業務名ラベル（1段目）、続くテキストが記事タイトル（2段目）。タイトルはリンク先記事の実タイトルにする。**`target="_blank" rel="noopener"` を付け別タブで開く**（読者が記事を読む途中で開いても読み位置を失わないため）。
  - 狙い：①読者の自分ごと化（「あ、うちのことだ」）②記事クラスタを内部リンクで"面"にしてSEOを底上げ③一般論から「業務システム化」文脈への橋渡し。
  - 抽象テーマ（属人化・DX・業務改善など）の記事ほど効果が大きい。テーマに合う業務だけ挙げる（無関係な業務を並べない）。
- **Before/After比較表を1つ入れる（推奨）**
  - 「現状（手作業・属人化）」vs「システム化後」を2〜4行の表で示し、文章だけの単調さを減らす。抽象テーマで特に有効。
- **タイトルは検索の軸キーワードを据え置く（注意）**
  - Search Consoleで表示が出ている軸キーワード（例：`中小企業 属人化`）を、装飾語で薄めたり言い換えたりしない。Excel・業務システム文脈は**タイトルではなく本文（業務例セクション等）で自然に**足す。本命キーワードのマッチを崩すと順位を落とすリスクがある。
- **「テーブルの変更前後を保持する監査ログ系機能」だけを想起させない（2026-06-30追加）**
  - **避けるべきはこれ1点だけ**：読者に「**テーブルの変更前後（紐づく関連テーブルの変更も含めて）を保持し、いつ・誰が・どう変えたかの差分がまるごと残る／たどれる**」という監査ログ系の機能を想起させること。これは全テーブルの変更前後＝差分を保持する仕組みが必要で実装コストが高く、安易に約束すると見積・実装で大きなギャップになる。
  - 機能一覧（## システム化でできること）に書かないのはもちろん、**課題セクションの本文でも「Excelは変更の経緯が残らない＝システムなら残せる」と匂わせない**（暗にこの監査ログを約束することになる）。
  - **逆に、これさえ想起させなければOK。** 「対応履歴・商談履歴・日報の記録」「見積の改訂版管理」「いつ・誰が・何を入力／作業したかの記録」のように**ユーザーが入力したデータを時系列で残すだけのもの**は実装が軽いので問題なく書いてよい。判断軸は「データの変更前後（差分）を自動で追跡するか」——する＝NG、入力を残すだけ＝OK。迷ったら書かない。
- **（将来課題・未ルール化）記事別CTA**：CTA文言を記事テーマに寄せると問い合わせ率は上がるが、現状CTAは共有コンポーネント（`blog/[slug].astro`・Layout）に直書き。frontmatterにCTA文言フィールドを足すテンプレ改修が必要で、1人運用方針とのトレードオフがあるため、引き継ぎ方針と相談してから判断する。

#### 商売上マイナスになる記述の禁止（2026-07-04追加）

費用・保守系記事のレビューで以下が繰り返し削除された。今後は最初から書かない：

- **既製品・クラウドツールへの誘導**（「既製品で足りるか先に確認」「月額ツールで済む場合も」等）→ 他社サービスに流す記述はNG
- **解約・契約終了の話題**（「保守契約は途中でやめられるか」等）→ 解約をイメージさせるFAQ・記述はNG
- **読者に交渉材料を与える記述**（「実費と作業費の内訳で妥当性を判断」「線引きを契約前に確認せよ」等）→ 内訳開示・確認行動の推奨はNG
- **約束しすぎる保守範囲**（OS/ミドルウェアのアップデート対応等）→ 保守内容はサーバー維持・バックアップ・不具合対応・軽微な調整・SSL更新までに留める
- ヒアリング項目（相談前に準備しておくとスムーズなもの）に「業務停止時の影響度」のような重い質問を入れない

**趣旨：** 記事は集客資産であり、読者への中立的アドバイスより「ビズシスへの相談につながるか」を優先する。事実を偽るのではなく、**読者を他社・自前解決・値下げ交渉へ誘導する記述を書かない**ということ。

### frontmatter 仕様

```markdown
---
title: "記事タイトル（SEOキーワードを含める・できるだけ具体的な数値を入れる）"
date: YYYY-MM-DD
tags: [タグ1, タグ2]
icon: "icon-gears.png"
author: "ビズシス"
description: "記事の説明（70〜90文字程度。X自動投稿にも使用されるため、X投稿として自然な文章にする）"
---
```

> **タイトル・descriptionの方針：できるだけ具体的な数値を入れる（2026-06-30追加）**
>
> 「具体ステップ」より「**具体的な4ステップ**」、「複数の機能」より「**6つの機能**」のように、
> ステップ数・機能数・件数・年数・割合などの**具体的な数値**を入れると、記事の中身が一目で伝わり検索結果でのクリック率（CTR）が上がりやすい。
> - **タイトルに優先して入れる**（検索結果で最初に目に入るため効果が大きい）。続けてdescriptionにも入れる。
> - 記事本文の実態と一致する数値にする（4ステップと書くなら本文も4ステップ）。
> - **タイトルとdescriptionは同じ数値で揃える**（例：タイトル「具体的な4ステップ」→descriptionにも「4ステップ」、本文H2見出しも「4ステップ」）。
> - 数値は半角（`4` `6` `80万円`）で書く。
> - 数値が自然に入れられないテーマで無理に作らない。入れられる場合は優先して入れる。

**`icon:` に指定できる画像ファイル一覧**（`/public/images/blog/` に格納）

| ファイル名 | 用途イメージ |
|---|---|
| `blog-thumb-default.png` | **汎用サムネ（初回はこれを設定する）** |
| `thumb-excel-exit.png` | Excel脱却・注目記事系 |
| `icon-gears.png` | 自動化・DX |
| `icon-people-network.png` | 属人化・チーム |
| `icon-clipboard.png` | チェックリスト・発注 |
| `icon-chart-up.png` | 効率化・メリット |
| `icon-handshake.png` | 会社選び・パートナー |
| `icon-cycle.png` | 保守・運用・継続 |
| `icon-boxes.png` | 在庫管理・Excel管理 |
| `icon-shield-check.png` | セキュリティ・DX入門 |

> **ルール：** 新しいブログ記事を作成した初回は必ず `icon: "blog-thumb-default.png"` を設定する。
> 記事に合うアイコンが決まったタイミングで上記一覧の中から適切なものに差し替える。
> `icon:` を省略した場合は `icon-gears.png` がフォールバックで表示される。

### ファイル命名規則（2026-06-30更新：日付なしスラッグへ移行）

- **形式：`記事スラッグ.md`（ファイル名に日付を入れない）**
- スラッグはローマ字・英数字・ハイフンのみ
- 例：`excel-zaiko-kanri-kaiketsu.md` → URLは `/blog/excel-zaiko-kanri-kaiketsu`
- 保存先：`site/src/content/blog/`

> **なぜ日付をファイル名から外したか（2026-06-30）**
>
> 公開URLは「ファイル名から `.md` を除いたもの」になる（`blog/[slug].astro` の `getStaticPaths`）。
> 旧形式 `YYYY-MM-DD-スラッグ.md` だと **URLに日付が露出し、数年後に古く見える／evergreen記事をリフレッシュしてもURLが当時の日付のまま** という弱点があった。
> 一方、**予約投稿フィルタ・並び順・関連記事はすべて frontmatter の `date:` を見ており、ファイル名の日付は一切参照していない**（コード確認済み）。
> そのため**ファイル名から日付を外してもドリップ公開・並び順・関連記事はそのまま動く**。公開日・表示日は frontmatter の `date:` で管理する。
>
> - **新規記事は日付なしスラッグで作成する。**
> - 公開日の制御（ドリップ公開含む）は必ず frontmatter の `date:` で行う（ファイル名ではない）。
> - **2026-06-30に旧33本も日付なしへ統一済み**（全ブログ記事が日付なしスラッグ）。旧URL（`/blog/YYYY-MM-DD-スラッグ`）は `site/astro.config.mjs` の `redirects` で新URLへ転送している。
> - GitHub Pages配信のため真の301は使えず、Astroの静的リダイレクト（`meta refresh` ＋ `canonical` ＋ 旧ページ `noindex`）が生成される。これがGitHub Pagesで取れる最善のSEOリダイレクト。
> - **新規記事は必ず日付なしスラッグで作る**（旧形式で作らない）。`redirects` への追記は不要。
> - 旧記事のスラッグを今後さらに変える場合は、同様に `redirects` に1行追加して旧URL→新URLを維持する。

---

## 3. Claude Code へのプロンプトテンプレート

以下をそのままコピーして、Claude Code のチャットに貼り付けてください。
`[　]` 内の項目だけ書き換えてから送信します。

---

```
以下の仕様に従って、ビズシスのブログ記事の下書きを1本生成してください。

## 記事の基本情報

- キーワード（SEO）：[例：Excel 在庫管理 限界]
- 想定タイトル（案）：[例：Excel在庫管理はもう限界？中小企業が最初に試すべき改善ステップ]
- 記事テーマのメモ：[例：Excel で在庫を管理していると、更新漏れや二重入力が起きやすい。具体的な改善方法を紹介したい]
- 今日の日付：[例：2026-04-15]
- ファイル名（スラッグ部分）：[例：excel-zaiko-kanri-kaiketsu]

## ビズシスの概要（記事内で参照してください）

- サービス名：ビズシス
- サービス概要：中小企業向けの業務システム導入・業務改善支援
- サイト URL：https://bizsys.jp
- 問い合わせページ：https://bizsys.jp/contact

## ターゲット読者

- 中小企業の経営者・管理職（非エンジニア）
- IT リテラシーは低め
- Excel 管理の限界を感じているが、何から始めるかわからない

## トーン・スタイル

- 親しみやすく、専門的すぎない
- 「難しそう」という印象を取り除く
- 結論→理由→具体例の順で書く
- 専門用語は使わない。使う場合はかっこ内に簡単な説明を入れる

## 記事構成（この順序で書いてください）

1. はじめに（読者の課題を提示。100〜150文字）
2. 課題の具体例（「あるある」な状況の描写）
3. 解決策の提示（ビズシスのアプローチ・具体的なステップ）
4. まとめ・次のアクション（要約 + 問い合わせへの誘導）

## 出力フォーマット（必ずこの形式で出力してください）

ファイル全体を以下の形式で出力してください。frontmatter から本文まで一つのコードブロック（```markdown）に含めること。

\`\`\`markdown
---
title: "（生成したタイトル）"
date: （今日の日付：YYYY-MM-DD形式）
tags: [（2〜3個のタグ）]
icon: "（上記一覧から内容に合うものを選ぶ）"
author: "ビズシス"
description: "（70〜90文字の説明文。X投稿として自然な文章にする）"
---

## はじめに

（本文）

## 課題の具体例

（本文）

## 解決策の提示

（本文）

## まとめ・次のアクション

（本文）
\`\`\`

出力後に、ファイル名の推奨案も1行で教えてください（形式：YYYY-MM-DD-スラッグ.md）。
```

---

## 4. 使い方手順（ステップ・バイ・ステップ）

### Step 1：Claude Code を開く

VS Code のターミナルで以下を実行するか、Claude Code のアイコンをクリックして起動します。

```bash
claude
```

### Step 2：プロンプトを貼り付けて記事を生成する

1. 上記「3. プロンプトテンプレート」のコードブロック内の文章を**全文コピー**する
2. Claude Code のチャット入力欄に貼り付ける
3. `[　]` の項目を書き換える（キーワード、タイトル案、日付など）
4. Enter で送信する

### Step 3：生成内容を確認する

Claude Code が生成した Markdown（` ```markdown ` ブロック内）を確認します。
問題があれば「〇〇を修正してください」と追加で指示を送ります。

### Step 4：ファイルに保存する

1. `site/src/content/blog/` フォルダを開く
2. 推奨ファイル名（例：`2026-04-15-excel-zaiko-kanri-kaiketsu.md`）で新規ファイルを作成する
3. 生成された ` ```markdown ` ブロックの**中身だけ**（` ```markdown ` と ` ``` ` の行を除いた部分）を貼り付けて保存する

### Step 5：push して公開する

ターミナルで以下を実行します：

```bash
cd c:\Dev\bizsys-pj
git add site/src/content/blog/（作成したファイル名）
git commit -m "feat: ブログ記事を追加 — （記事タイトル）"
git push origin main
```

push 後、GitHub Actions が自動でサイトをデプロイし、X にも自動投稿されます。

> **⚠ 予約投稿の仕組み（重要）**
>
> 本番サイトでは「記事の日付（`date`）が**今日以前**のものだけ」が公開されます。
> **未来の日付にした記事は、push してもすぐには公開されず、その日付になると自動で公開されます（予約投稿）。**
>
> - **すぐ公開したい** → `date` を**今日以前**にする
> - **順次公開したい** → 複数記事を**未来の日付で1日ずつずらす**と、1日1本ペースで自動公開できる（ドリップ公開）
> - dev サーバー（`npm run dev`）では未来日付の記事も表示されます。除外されるのは**本番ビルド時のみ**です
> - **記事を追加したのにサイトに出てこないときは、まず `date` が未来になっていないか確認してください**（不具合ではなく仕様です）
>
> 実装箇所：`site/src/pages/blog/index.astro` の日付フィルタ（`blog/[slug].astro`・`blog/tag/[tag].astro` にも同じフィルタあり）。

---

## 5. 生成品質のチェックポイント

push 前に以下を確認してください。

| チェック項目 | 確認方法 |
|------------|---------|
| frontmatter が正しく記述されているか | `---` で囲まれた部分がファイルの先頭にあるか確認 |
| `title` に SEO キーワードが入っているか | 読者が検索しそうな言葉が含まれているか |
| `title` に具体的な数値が入っているか | ステップ数・機能数などを入れられるなら入れる（最初に目に入りCTRが上がる。本文・descriptionと数値を揃える） |
| `description` が 70〜90 文字か | 文字数をコピーして確認（短すぎ・長すぎに注意） |
| `description` が X 投稿として自然か | 読んで違和感がないか |
| `description` に具体的な数値が入っているか | ステップ数・機能数・件数などを入れる。タイトルに数値があれば同じ数値を揃える（本文の実態と一致させる） |
| `date` が正しい日付形式か | `YYYY-MM-DD`（例：`2026-04-15`） |
| `icon` が指定されているか | 上記アイコン一覧から内容に合うファイル名を選んで記述 |
| `author` が `"ビズシス"` になっているか | そのまま確認 |
| ファイル名がルール通りか | **`スラッグ.md`（日付なし）**、スラッグはローマ字・英数字・ハイフンのみ。公開日は frontmatter の `date:` で管理 |
| 記事が規定の構成になっているか | はじめに／課題／解決策／**機能一覧／小さく始める／相談前準備**／FAQ／まとめ |
| 本文が3,500文字程度か | 空白・関連記事を除いてカウント |
| まとめに問い合わせへの誘導があるか | `/contact` へのリンクまたは言及 |
| まとめ末尾に関連ページ導線があるか | サービス/事例/デモ/料金（**相対パス**。事例は近い事例がある時だけ） |

---

## よくあるトラブル

**Q. 生成内容が長すぎる・短すぎる**  
A. 「はじめに を 100〜150 文字に収めてください」のように、セクション単位で修正を依頼してください。

**Q. description が 90 文字を超えてしまう**  
A. 「description を 80 文字以内に短くしてください」と追加指示を出してください。

**Q. X 投稿の文章として不自然**  
A. 「description を X（Twitter）の投稿として自然な文体に書き直してください」と依頼してください。
